diff --git a/lib/index.js b/lib/index.js
index b2cadf0..79ce466 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -365,5 +365,19 @@ async function* translate(payloads) {
 	const order = [];
 	let pendingFinish;
 	let pendingUsage;
+	async function* terminalCompletionPayloads() {
+		try {
+			yield* payloads;
+		} catch (error) {
+			// Some OpenAI-compatible gateways omit [DONE] after sending an
+			// explicit finish_reason. Accept only that completed shape; a stream
+			// that closes before a finish_reason remains retryable STREAM_CLOSED.
+			if (error instanceof LlmError && error.code === "STREAM_CLOSED" && pendingFinish !== void 0) {
+				yield "[DONE]";
+				return;
+			}
+			throw error;
+		}
+	}
 	function open(kind) {
 		const block = {
@@ -374,4 +388,4 @@ async function* translate(payloads) {
 		return block;
 	}
-	for await (const payload of payloads) {
+	for await (const payload of terminalCompletionPayloads()) {
 		if (payload === "[DONE]") {
@@ -410,5 +410,6 @@ async function* translate(payloads) {
 		for (const choice of chunk.choices ?? []) {
 			const delta = choice.delta;
+			const nativeToolCalls = delta?.tool_calls ?? [];
 			const reasoning = delta?.reasoning_content;
 			if (typeof reasoning === "string" && reasoning.length > 0) {
 				if (!reasoningBlock) {
@@ -426,7 +427,8 @@ async function* translate(payloads) {
 				};
 			}
 			const content = delta?.content;
-			if (typeof content === "string" && content.length > 0) {
+			const legacyToolEnvelope = typeof content === "string" && content.trimStart().startsWith("<｜DSML｜tool_calls>");
+			if (typeof content === "string" && content.length > 0 && !legacyToolEnvelope) {
 				if (!textBlock) {
 					textBlock = open("text");
 					yield {
@@ -442,7 +444,7 @@ async function* translate(payloads) {
 					text: content
 				};
 			}
-			for (const call of delta?.tool_calls ?? []) {
+			for (const call of nativeToolCalls) {
 				let block = toolBlocks.get(call.index);
 				if (!block) {
 					block = open("tool-call");
@@ -452,8 +454,8 @@ async function* translate(payloads) {
 						blockType: "tool-call"
 					};
 				}
-				if (call.id !== void 0) block.callId = call.id;
-				if (call.function?.name !== void 0) block.name = call.function.name;
+				if (call.id) block.callId = call.id;
+				if (call.function?.name) block.name = call.function.name;
 				const fragment = call.function?.arguments ?? "";
 				block.text += fragment;
 				yield {
@@ -641,6 +641,32 @@ var DeepSeekAdapter = class extends LlmAdapter {
 	providerRetryPolicy(_provider) {
 		return this.config.options().retryPolicy;
 	}
-	listModels(provider) {
-		return Promise.resolve(this.config.options().models.map((model) => modelInfo(provider, model)));
+	async listModels(provider) {
+		const connection = this.config.options();
+		const fallback = () => connection.models.map((model) => modelInfo(provider, model));
+		const endpoint = new URL(`${connection.baseURL.replace(/\/+$/, "")}/models`);
+		endpoint.searchParams.set("tag", "2");
+		const apiKey = await this.config.resolveApiKey(connection);
+		let response;
+		try {
+			response = await fetch(endpoint, {
+				headers: {
+					"authorization": `Bearer ${apiKey}`,
+					"accept": "application/json"
+				}
+			});
+		} catch {
+			return fallback();
+		}
+		if (!response.ok) return fallback();
+		let payload;
+		try {
+			payload = await response.json();
+		} catch {
+			return fallback();
+		}
+		if (payload?.ability_tag !== "2" || !Array.isArray(payload.data)) return fallback();
+		const configured = new Map(connection.models.map((model) => [model.id, model]));
+		const ids = [...new Set(payload.data.map((model) => model?.id).filter((id) => typeof id === "string" && id.length > 0 && id !== "gpt-5.6-luna"))];
+		return ids.length === 0 ? fallback() : ids.map((id) => modelInfo(provider, configured.get(id) ?? { id }));
 	}
@@ -762,2 +804,7 @@ var DeepSeekAdapter = class extends LlmAdapter {
+		const contentType = response.headers.get("content-type")?.split(";", 1)[0]?.trim().toLowerCase();
+		if (contentType && contentType !== "text/event-stream") {
+			await response.body?.cancel().catch(() => {});
+			throw new LlmError("模型接口返回的不是 SSE 流式响应。请检查 baseURL 是否包含正确的 API 路径；锐捷登录请恢复默认地址。", "MALFORMED_RESPONSE");
+		}
 		if (!response.body) throw new LlmError("DeepSeek API returned no response body", "EMPTY_RESPONSE");
 		yield* translate(parseSse(response.body, onComment));
